calculating the triangular series

  var num = 5;
  ((num * num) + num) / 2;

    => 15


get the length of a number without converting it to a string

  var number = 112;
  Number.parseInt(Math.log10(number)+1)
    => 3

****************************************************************************************************
javaScript

  Math.random() * (max - min) + min;
